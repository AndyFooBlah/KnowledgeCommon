{
  "$comment": "Allow-list of Gemini model IDs KnowledgeCommon may call by default. Verified against https://ai.google.dev/gemini-api/docs/models on 2026-09-10. `npm test` asserts DEFAULT_MODELS are listed here; `node scripts/check-models.mjs` re-verifies every entry against the live /v1beta/models endpoint when GEMINI_API_KEY is set.",
  "verified": "2026-09-10",
  "models": [
    "gemini-3.5-flash-lite",
    "gemini-embedding-001"
  ]
}
